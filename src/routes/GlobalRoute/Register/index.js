import React from "react";
import Authentication from "../../../layouts/Authentication";

const Register = () => {
  return (
    <Authentication>
      <div> This is the register route</div>
    </Authentication>
  );
};

export default Register;
